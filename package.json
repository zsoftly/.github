{
  "name": "zsoftly-dot-github",
  "version": "0.0.0",
  "private": true,
  "description": "ZSoftly organization profile and shared GitHub defaults",
  "scripts": {
    "fmt": "prettier --write \"**/*.{md,mdx,json,yml,yaml}\"",
    "fmt:check": "prettier --check \"**/*.{md,mdx,json,yml,yaml}\""
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
